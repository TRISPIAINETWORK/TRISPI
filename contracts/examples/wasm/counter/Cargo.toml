[package]
name = "trispi-counter"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[profile.release]
opt-level = "z"       # optimize for size (WASM)
lto = true
panic = "abort"
